# AUDIT 2026-09-03 — Tool Nutrizionale + Etichette

**Metodo:** test live su dev server (Vite 5173, `VITE_DEV_MOCK_AUTH=true`), desktop 1440×900 + mobile 375×812, più lettura del codice. Perimetro: `src/calculators/NutrizionaleCalc/`, `src/calculators/EtichetteCalc/`, `src/engines/nutrizionaleCalcEngine.ts` e le costanti condivise.

**Come usare questo file:** ogni finding ha un ID stabile (N1…N9, E1…E7, T1…T3, D1…D2). Spunta la casella quando è chiuso e aggiorna la riga **Stato**. All'inizio di una nuova sessione basta leggere questo file per sapere dove si era rimasti.

---

## Stato complessivo

| | Bloccanti | Alti | Minori | Igiene | Totale |
|---|---|---|---|---|---|
| Chiusi | 5/7 | 2/2 | 8/8 | 3/3 | **19/21** |

🟡 Parziali: **E3** (mitigato, fix strutturale aperto), **E4** (codice fatto, bloccato su D1).
🔴 Nuovo finding emerso durante i fix: **D1** — difetto nei dati del DB ingredienti, richiede una decisione.

Ultimo aggiornamento: 2026-09-03 — chiusi N1, N2, N2b, N3, N4, N5, N6, N7, N9, E1, E2, E3, E5, E6, T1, T2. Non confermati N8, T3, E7. Parziale E4 (bloccato su D1). Aperti **D1 e D2**: difetti nei dati dell'Excel sorgente, 13 celle in tutto.
**Suite 182/182 verde**, `tsc -b` pulito, lint 0 errori.
Branch di lavoro: `fix/audit-2026-09-03` (un commit per task, `main` intatto).

---

## Vincoli da rispettare durante i fix

- Il motore nutrizionale (`calcNutrients`, `scaleResult`, `calcQuid`, `energyFromMacros`) **non va toccato**: nessun finding lo richiede.
- Markup e stili di `Tab{UE,USA,Canada,Australia,Arabi}.tsx` sono tabelle ufficiali: modificarli solo dove il finding lo impone e senza variazioni estetiche.
- Ogni modifica a `src/engines/` richiede `npm test` verde prima di dichiarare chiuso il task.
- Ogni modifica TS/TSX richiede `npx tsc -b` e `npm run lint` puliti.

---

## 🔴 Bloccanti — l'app produce documenti formalmente non conformi

### [ ] E4 · Allergeni: GRANO e GLUTINE trattati come gruppi paritari
**Stato:** 🟡 **PARZIALE** 2026-09-03 — codice corretto e testato (commit `fix(E4)`), **bloccato su D1**: finché i 3 tag errati nel DB restano, la regola produce un falso allergene su prodotti senza glutine
**File:** [`shared/constants.ts:6-17`](src/calculators/NutrizionaleCalc/shared/constants.ts), consumato da [`EtichetteCalc.tsx:28`](src/calculators/EtichetteCalc/EtichetteCalc.tsx), aggregazione a `:1024` e `:1100`

All. II Reg. 1169/2011 punto 1 definisce **un solo** gruppo: "cereali contenenti glutine, vale a dire grano, segale, orzo, avena, farro, kamut". L'app espone `GLUTINE` e `GRANO` come due chip distinti. Stessa struttura per `FRUTTA A GUSCIO` / `ANACARDI`.

Conteggio sul DB reale (`public/data/ingredientsDB.json`, 1065 voci):

```
soloGrano: 85    soloGlutine: 11    entrambi: 22    soloAnacardi: 0
```

**85 ingredienti dichiarano `all_grano` senza `all_glutine`**: per quei prodotti l'etichetta generata omette il gruppo allergene obbligatorio. Il caso anacardi è oggi inerte (0 occorrenze).

Incoerenze collegate da sanare nello stesso task:
- `CustomIngredientModal.tsx:8` non include `all_grano` → un ingrediente creato a mano non può dichiarare il grano.
- `EtichetteCalc.compliance.test.ts:25` copre un set di campi diverso da quello del DB.

**Rischio regressione: ALTO** — cambia l'insieme allergeni mostrato in entrambi i tool e nelle etichette già archiviate. Richiede `npm test` e verifica manuale su una ricetta salvata.

**Fix applicato:**
- Nuova mappa `ALLERGEN_PARENT` (`GRANO → GLUTINE`, `ANACARDI → FRUTTA A GUSCIO`) e funzione `collectAllergenLabels(ingredienti, campi, escludi?)` in `shared/constants.ts`. La voce di dettaglio **non** viene sostituita — indicare il cereale specifico è utile e conforme — ma trascina sempre con sé il gruppo All. II.
- I **4 punti di aggregazione** che ripetevano lo stesso `forEach` ora chiamano la funzione condivisa: `NutrizionaleCalc.tsx` (presenti + tracce), `NutrizionaleCalcMobile.tsx` (idem), `EtichetteCalc.tsx` (idem). La regola vive in un punto solo e non può divergere fra i tre tree.
- Le tracce continuano a escludere gli allergeni già presenti, e l'esclusione si applica **anche al gruppo derivato**: se GLUTINE è già presente, `cross_grano` aggiunge GRANO fra le tracce ma non ripete GLUTINE.
- `CustomIngredientModal.tsx`: le 4 liste ricopiate a mano (a cui mancavano `all_grano`/`cross_grano`) sono ora **derivate** da `ALLERGEN_FIELDS`/`CROSS_FIELDS`. Effetto collaterale voluto: la label della traccia solfiti passa da `SOLFITI (>10 ppm)` a `SOLFITI`, allineandosi al resto dell'app.

**Verifica:** 6 test nuovi, suite 176 pass / 2 fail (i 2 sono T1), `tsc -b` pulito.

⚠️ **Perché resta PARZIALE:** vedi **D1** qui sotto. Il codice è corretto, i dati no.

---

### [x] E1 · Soglia leggibilità confronta il corpo carattere invece dell'altezza della x
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(E1)`
**File:** [`EtichetteCalc.tsx:1579-1584`](src/calculators/EtichetteCalc/EtichetteCalc.tsx) (fronte), `:1638-1641` (retro), messaggi a `:2806` e `:2953`

```ts
const MIN_READABLE_MM = frontSurfaceCm2 < 80 ? 0.9 : 1.2;
const bodyFontSizeMm  = mmPerPx * (11 * fontScale);
const isBodyTextReadable = bodyFontSizeMm >= MIN_READABLE_MM;
```

All. IV fissa 1,2 mm (0,9 mm sotto gli 80 cm²) come **altezza della x**, non come corpo carattere. Per una sans-serif x-height ≈ 0,5× corpo → la stima sottovaluta di circa 2×.

Verificato live: a 40×30 mm l'app segnala `0.87mm — sotto la soglia`. A ~0,95 mm scriverebbe *"leggibile secondo Reg. UE 1169/2011"* con x-height reale ≈ 0,48 mm, cioè non conforme. **È un'asserzione di conformità falsa firmata AEA.**

> **Nota:** la skill `normativa-alimentare` dava Art. 13(2)+All. IV come già coperto ("✅ modulato 2026-08-25"). La sessione del 25/08 aveva reso **dinamica la soglia** (0,9 vs 1,2 su superficie) ma lasciato il confronto sul corpo carattere. E1 non è un gap nuovo: è la correzione della grandezza confrontata dentro quell'implementazione. Skill aggiornata di conseguenza.

**Fix applicato:**
- Nuova costante esportata `ARIAL_X_HEIGHT_RATIO = 1062/2048` (0,5186) con helper puro `xHeightMm(fontSizeMm)`. Entrambe le anteprime usano `Arial, sans-serif`, quindi il rapporto è una **costante del font**, non una stima: se l'anteprima cambia famiglia va ricalcolato (annotato nel commento).
- `isBodyTextReadable` e `isBackBodyTextReadable` confrontano ora l'altezza della x, non il corpo.
- Messaggi riscritti: da `Corpo testo (fronte) ≈ X mm — leggibile secondo Reg. UE 1169/2011` a `Altezza della x (fronte) ≈ X mm — ≥ soglia 1.2mm All. IV Reg. UE 1169/2011`. Il tooltip esplicita che la norma misura l'altezza della x, riporta anche il corpo da cui è derivata e avverte che la superficie usata è quella dell'etichetta (aggancio a E3).

**Verifica:** 4 test nuovi in `EtichetteCalc.compliance.test.ts` (46/46 verdi), `tsc -b` pulito. Live, stessa etichetta a formati diversi:

| Formato | Prima | Dopo |
|---|---|---|
| 40×30 mm | `0.87mm` sotto soglia | `0.45mm` sotto soglia (0,9) |
| 100×150 mm | ✅ verde | ⚠️ `1.13mm` **sotto soglia (1,2)** |
| 200×150 mm | ✅ verde | ✅ `2.26mm` |

⚠️ **Effetto atteso sul cliente:** al formato di default 100×150mm, con un contenuto realistico, l'etichetta ora **segnala** invece di dare il verde. È il comportamento corretto — quel verde era falso — ma è un cambiamento visibile: chi usa il default vedrà un avviso che prima non c'era. Da comunicare, non da nascondere.

---

### [x] N1 · Claim nutrizionali falsi su ricetta vuota
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N1)`
**File:** [`nutrizionaleCalcEngine.ts:235,238,241`](src/engines/nutrizionaleCalcEngine.ts) · render [`NutrizionaleCalc.tsx:953`](src/calculators/NutrizionaleCalc/NutrizionaleCalc.tsx) e [`mobile/TabellaTab.tsx:343`](src/calculators/NutrizionaleCalc/mobile/TabellaTab.tsx)

Con zero ingredienti la UI mostra `A BASSO CONTENUTO DI SODIO`, `DI ZUCCHERI`, `DI GRASSI`: `calcClaims` non ha guard sul risultato tutto-zero e `sodio_mg <= 120` con 0 è vero.

**Rischio regressione: BASSO** — `makeResult` nei test usa `energyKcal: 400, sodio_mg: 999` di default ([`nutrizionaleCalcEngine.test.ts:179`](src/engines/nutrizionaleCalcEngine.test.ts)), quindi nessun test parte da uno stato tutto-zero: un guard non li rompe.

**Fix applicato:** guard in testa a `calcClaims` che verifica *tutti e soli* i campi letti dalla funzione (`fibre, energyKcal, proteine, calcio, ferro, potassio, sodio_mg, zuccheri, grassi`): se nessuno è valorizzato ritorna `[]`. Scelto un guard unico nell'engine invece di due nei chiamanti, così desktop e mobile sono coperti insieme e lo è anche ogni chiamante futuro. Nessun ingrediente del DB azzera l'intero set — l'acqua ha sodio 4mg e calcio 7,13mg — quindi non sopprime claim legittimi.

**Verifica:** 2 test nuovi (`ricetta vuota → []`, `un solo valore valorizzato → claim "a basso contenuto" attivi`); engine 45/45 verde; `tsc -b` pulito; suite 166 pass / 2 fail (i 2 sono T1, preesistenti). Verificato live: ricetta vuota mostra "Nessun claim applicabile con i valori attuali", e con `farina di avena` i 6 claim precedenti sono invariati.

---

### [x] E2 · "Tutti i campi obbligatori compilati" verifica solo 4 campi
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(E2)`
**File:** [`EtichetteCalc.tsx:1204-1209`](src/calculators/EtichetteCalc/EtichetteCalc.tsx), messaggio a `:2494`

`requiredFields` copre denominazione, produttore, quantità netta, elenco ingredienti. Mancano dal controllo:

- TMC / data di scadenza — Art. 9.1.f
- Numero di lotto — D.Lgs 231/2017 (Dir. 2011/91/UE)
- Dichiarazione nutrizionale — Art. 9.1.l (salvo esenzioni All. V)
- Condizioni di conservazione — Art. 25, quando applicabili

Verificato live: etichetta senza TMC né lotto ottiene la spunta verde e abilita tutti e tre gli export.

**Fix applicato:**
- `requiredFields` esteso con TMC/scadenza, numero di lotto e dichiarazione nutrizionale, ciascuno con la propria esenzione: lotto escluso se il TMC riporta giorno e mese (Dir. 2011/91/UE Art. 1(3), stessa condizione della nota già presente in UI), dichiarazione nutrizionale esclusa sotto i 25 cm² (All. V p.18), elenco ingredienti escluso sotto i 10 cm² (Art. 16(2)).
- `frontSurfaceCm2` e le due esenzioni per superficie **spostate sopra** `requiredFields`: erano definite 370 righe più in basso e servivano al controllo. `MIN_READABLE_MM` resta dov'era.
- Le lettere condizionali dell'Art. 9(1) — allergeni, QUID, origine, istruzioni d'uso, titolo alcolometrico — **non** sono state rese bloccanti: l'app non può sapere se il caso si applica, e bloccare l'export produrrebbe falsi impedimenti. Sono dichiarate esplicitamente sotto il banner come "da verificare a mano".
- Conservazione (Art. 9(1)(g) + Art. 25): avviso arancione cliccabile che porta al campo, **senza** bloccare l'export — è condizionale al prodotto.
- Copy del banner da "Tutti i campi obbligatori compilati" a **"Menzioni obbligatorie compilate"**: la vecchia formula affermava una completezza che il controllo non può garantire.
- `FIELD_SECTION` aggiornata coi nuovi id, così il click su un campo mancante apre la sezione giusta; aggiunto `id="et-tabella-nutrizionale"`.

**Verifica:** `tsc -b` pulito, 42/42 test compliance verdi. Live: compilando solo i 4 campi storici l'app ora segnala `Mancano: TMC / data di scadenza, Numero di lotto, Dichiarazione nutrizionale`; a 40×30mm cade la dichiarazione nutrizionale; compilando il TMC con giorno cade il lotto; a completamento appare l'avviso conservazione e i 3 export si abilitano.

---

### [x] N3 · Etichette USA/Canada/Arabi esportabili con serving size 0 g
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N3,N6,N7)`
**File:** `TabUSA.tsx`, `TabCanada.tsx`, `TabArabi.tsx` · gate di export in `DownloadTableModal.tsx`

Senza serving size le tabelle renderizzano `Serving size 0 g` / `Par 0g · pour 0g` e "Scarica ufficiale…" resta attivo → si scarica un pannello FDA/CFIA non valido. In più `1 servings per container` (plurale errato con valore 1).

**Fix applicato:** gate in `DownloadTableModal` — **non** nei `Tab*.tsx`, che sono file protetti e che comunque non sono il punto giusto: il modal è l'unico passaggio da cui esce un download ufficiale. Per USA, Canada, Australia e Arabi, se manca la porzione il bottone "Scarica PNG" è disabilitato con spiegazione accanto e `title`; `handleDownload` ha lo stesso guard, così il blocco non dipende dallo stato della UI. L'UE è esclusa di proposito: la sua base è per 100g/100ml e la porzione è volontaria (Art. 33).

**Verifica live:** su USA senza porzione il bottone è `disabled` con il messaggio *"Indica la porzione per questo mercato…"*; compilando `Serving = 30` la tabella mostra `Serving size 30 g` e il bottone si sblocca; su UE il download resta sempre abilitato (nessuna regressione).

⚠️ **Non risolto qui:** il plurale `1 servings per container` sta dentro `TabUSA.tsx`/`TabArabi.tsx`, file protetti. Vedi la sezione *Da fare alla fine* in fondo.

---

### [ ] D1 · 🔴 DATI — `grano saraceno` taggato come grano nel DB ingredienti
**Stato:** aperto — **bloccato: la sorgente non è in questo repo e non è raggiungibile da questa macchina**
**File:** `public/data/ingredientsDB.json` (generato), sorgente reale: file Excel esterno

**Sorgente individuata (2026-09-03, percorso fornito dall'utente):**

```
.../PRODOTTI DIGITALI/programma etichette, schede tecniche, processo, costi/
    programma etichette, ... -con calcoli/Programma etich., sch. proc., sch. tecn., c costi-calcolo.xlsx
```

foglio **`database`**. È davvero la sorgente del DB dell'app: **1057 nomi su 1065** combaciano confrontando la colonna nome dell'Excel con il campo `etichetta` del JSON (sul campo `nome` ne combaciano solo 342, perché l'Excel tiene la dichiarazione completa dove l'app tiene il nome breve — è una differenza di convenzione, non di contenuto).

Il modello allergeni dell'Excel **conferma la gerarchia di E4**: `GLUTINE` è il gruppo, con colonne separate per `GRANO / SEGALE / ORZO / AVENA / FARRO`; `FRUTTA A GUSCIO` ha sotto mandorle, noci, nocciole, pistacchi, anacardi, pecan, brasile, macadamia.

**Colonne rilevanti:** `HM` = GLUTINE presente · `HN` = GLUTINE tracce · `HO` = GRANO presente · `HP` = GRANO tracce.

**Celle da correggere — grano saraceno (colonna `HO`, togliere l'1):**

| Riga Excel | Nome |
|---|---|
| 331 | farina di grano saraceno |
| 485 | grano saraceno |
| 824 | preparato a base di grano saraceno con tartufo |
| 625 | mix … senza glutine (contiene farina di grano saraceno) — vedi anche D2 |

⚠️ **Non ho modificato l'Excel.** È un file da 15 MB con 23 fogli e formule: riscriverlo con una libreria significa rigenerare l'intera cartella di lavoro e perdere formule e formattazione. Sono 4 celle, si fanno a mano in un minuto senza alcun rischio.

**Contesto utile:** in tutto il foglio ci sono **89 righe con `GRANO` valorizzato e `GLUTINE` no**. Le altre 85 sono grano vero (farina tipo 0/00/1/2, crusca, germe, babà, bignè, crostini) e il fix E4 le corregge automaticamente. Solo queste 4 sono errori di dato.

**Conseguenze, in ordine di gravità:**
1. **Già oggi, prima di qualsiasi mia modifica**, questi ingredienti fanno dichiarare `GRANO` come allergene presente: è una falsa dichiarazione di allergene.
2. **Dopo E4** la regola dei gruppi vi aggiunge `GLUTINE`. Il difetto non nasce dal fix — il fix lo rende più visibile e più dannoso.

Il terzo caso (`preparato per pane…`) è ambiguo: un preparato per pane al grano saraceno **può** contenere glutine aggiunto. Va deciso sul prodotto reale, non per analogia.

**Altre voci da controllare nella stessa passata** (`all_grano` senza `all_glutine`, plausibili ma da confermare): `preparato a base di farina di mais`, `miglioratore per panificazione a base enzimatica`, `miglioratore per prodotti da forno … granaio delle idee`. Sul totale, 85 voci su 1065 hanno `all_grano` senza `all_glutine`: le altre 82 sono grano vero (farine tipo 00/0/1/2, crusca, germe, pane) e il fix E4 le corregge.

**Opzioni:**
- **(a)** Correggere il DB alla fonte (foglio/pipeline che genera `ingredientsDB.json`): togliere `all_grano` alle due voci di grano saraceno, decidere sulla terza. È la strada giusta — il difetto è nel dato.
- **(b)** Eccezione nel codice per nome: fragile, si rompe al primo nome nuovo. Sconsigliata.
- **(c)** Non fare nulla: resta una falsa dichiarazione di allergene su farina per celiaci. Da escludere.

---

### [ ] D2 · 🔴 DATI — 9 prodotti "senza glutine" dichiarati come contenenti glutine
**Stato:** aperto — emerso 2026-09-03 analizzando l'Excel sorgente per D1. **Più grave di D1**: sono 9 prodotti, non 3.
**File:** stesso foglio `database`, colonna `HM` (GLUTINE presente)

Prodotti il cui nome dichiara "senza glutine" / "gluten free", con `GLUTINE` valorizzato come **presente** benché la lista ingredienti non contenga alcun cereale del gruppo:

| Riga Excel | Prodotto (troncato) |
|---|---|
| 120 | biscotti senza glutine (amido di mais, amido di riso, amido di tapioca…) |
| 625 | mix ai cereali e semi per prodotti da forno senza glutine |
| 629 | preparato per prodotti lievitati da forno senza glutine |
| 632 | preparato per prodotti da forno senza glutine (amido di riso…) |
| 830 | preparato per prodotti senza glutine (amido di mais, farina di riso…) |
| 832 | preparato per prodotti da forno senza glutine |
| 833 | preparato per prodotto da forno senza glutine |
| 834 | preparato per prodotti da forno senza glutine (amido di riso…) |
| 936 | premiscelato per frolla senza glutine (farina di riso, zucchero…) |

**Il difetto arriva all'app**: in `ingredientsDB.json`, `biscotti senza glutine` ha `all_glutine: 1`. Chi lo usa in una ricetta si vede dichiarare **GLUTINE** come allergene presente su un prodotto certificato senza glutine — l'errore opposto a D1 e commercialmente pesante.

**Ipotesi più probabile:** confusione di colonna, `HM` (presente) al posto di `HN` (tracce). Molti preparati gluten-free dichiarano legittimamente "può contenere tracce di glutine". Da confermare prodotto per prodotto sulle schede tecniche del fornitore — non è una correzione che posso fare a occhio.

**Casi verificati come CORRETTI, da non toccare:**
- riga 56 `amido di frumento deglutinato` — contiene frumento; All. II non lo esenta (l'esenzione copre sciroppi di glucosio e maltodestrine a base di grano, non l'amido). Dopo E4 il gruppo `GLUTINE` viene aggiunto automaticamente ed è giusto così.
- righe 829 e 831 — contengono **avena**, che è nel gruppo "cereali contenenti glutine" (All. II p.1). `GLUTINE` presente è corretto, `GRANO` giustamente no.

---

## 🟠 Alti

### [x] N2 · Tab Australia inutilizzabile senza serving size
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N2,N2b,T1)`, su autorizzazione esplicita per i file protetti
**File:** [`TabAustralia.tsx:56-77`](src/calculators/NutrizionaleCalc/TabAustralia.tsx) (costruzione `rows`), render a `:113`

`rows` viene popolato solo dentro `if (sv)`. Senza serving size la NIP mostra header + il messaggio "Inserire il valore serving size", e resta vuota anche la colonna **Average Quantity per 100 g**, che è obbligatoria FSANZ e non dipende dalla porzione.

**Fix applicato** (file protetto — modifica minima, nessuna variazione di stile, bordi, font o struttura): le righe si costruiscono sempre; restano vuote le sole due colonne per-porzione finché la porzione manca. Il messaggio placeholder resta come riga finale, riformulato in "…per compilare le colonne per porzione" (prima diceva "per calcolare le quantità", ora le quantità per 100 g ci sono già).

**Verifica live:** senza porzione la NIP mostra `Energy 1620 kJ (384 Cal)`, `Protein 13.2 g`, `Fat, total 6.3 g`, `Sodium less than 5 mg` nella colonna per 100 g. Arrotondamenti FSANZ invariati.

---

### [x] E3 · Soglie di superficie calcolate sull'etichetta, non sull'imballaggio
**Stato:** ✅ CHIUSO 2026-09-03 — mitigazione (commit `fix(E3,T2)`) + fix strutturale (commit `fix(E3-full,D1-doc)`)
**File:** [`EtichetteCalc.tsx:1579`](src/calculators/EtichetteCalc/EtichetteCalc.tsx) (`frontSurfaceCm2`), messaggio esenzione a `:2814`

`frontSurfaceCm2` è base × altezza dell'etichetta. Da lì dipendono sia la scelta 0,9 vs 1,2 mm sia il messaggio *"Superficie ≈12cm² <25cm²: dichiarazione nutrizionale non obbligatoria (All. V p.18)"*. La norma parla della **superficie maggiore dell'imballaggio**: un'etichetta 40×30 su una scatola grande fa suggerire un'esenzione inesistente.

**Fix completo** = campo nuovo (superficie maggiore imballaggio) + migrazione delle etichette archiviate.
**Fix interim** = trasformare l'affermazione in stima esplicita, senza toccare lo schema.

**Mitigazione applicata (2026-09-03):** i due banner di esenzione erano **verdi con spunta**, quindi si leggevano come un via libera accertato. Ora sono arancioni con icona di avviso e riformulati come condizionali: *"Etichetta ≈12cm². **Se anche la superficie maggiore dell'imballaggio** sta sotto i 25cm², la dichiarazione nutrizionale non è obbligatoria… Qui è confrontata la misura dell'etichetta, non dell'imballaggio: verificalo prima di ometterla."* Stesso trattamento per l'avviso Art. 16(2) (<10cm²). Il tooltip della leggibilità (E1) porta lo stesso avvertimento sulla soglia 0,9/1,2mm. Verificato live a 40×30mm e 25×25mm.

**Fix strutturale applicato (2026-09-03):**
1. Nuovo campo facoltativo `packageSurfaceCm2` in `LabelData`, con input dedicato nella scheda Grafica sotto le dimensioni etichetta e tooltip che spiega perché la norma misura lì.
2. `frontSurfaceCm2` ora vale la superficie **dell'imballaggio quando è dichiarata**, altrimenti quella dell'etichetta. Ne derivano insieme `MIN_READABLE_MM` (E1), `isNutritionDeclarationExempt`, `isMostFieldsExempt` e — tramite `requiredFields` — anche E2.
3. **Retrocompatibilità:** il campo è opzionale (`packageSurfaceCm2?: string`) e letto con `?? ''`. Le etichette archiviate prima di oggi non ce l'hanno e si comportano esattamente come prima. Nessuna migrazione necessaria.
4. Quando il campo è vuoto, un avviso inline sotto l'input dichiara su quale superficie stanno ragionando le soglie; i due banner di esenzione cambiano testo di conseguenza (condizionale se si usa l'etichetta, affermativo se si usa l'imballaggio).

**Verifica live** (etichetta 40×30mm = 12cm²): senza imballaggio dichiarato compare l'esenzione condizionale <25cm²; dichiarando **400 cm²** l'esenzione **sparisce**, che è il caso di falsa esenzione descritto sopra. Con etichetta 100×150mm (150cm²) e imballaggio **20 cm²**, la soglia di leggibilità passa a **0,9mm** (`Altezza della x ≈ 1.51mm — ≥ soglia 0.9mm`) e la dichiarazione nutrizionale esce dai campi obbligatori: E1, E2 ed E3 si muovono insieme e in modo coerente.

---

## 🟡 Minori — attrito commerciale, non legale

### [x] N4 · Tab Mercati mobile bianca di default
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N4,N5,N9,E5,E6)` · **File:** [`mobile/TabellaTab.tsx:159`](src/calculators/NutrizionaleCalc/mobile/TabellaTab.tsx), render a `:313`

`useState<Region | null>(initialRegion ?? null)` con `{selectedRegion && hasData && …}` e nessun ramo else → schermata vuota senza spiegazione. Desktop parte da EU: rottura di parità.

**Fix applicato:** default `'UE'` invece di `null`, come il desktop. I chip regione non permettono la deselezione, quindi dopo il mount lo stato non torna mai `null`. Il messaggio per il caso "nessun ingrediente" esisteva già ed è invariato. **Verificato live:** la scheda Mercati mobile si apre sulla dichiarazione UE.

### [x] N5 · Ricerca ingredienti naive contro Fuse usato nell'import
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N4,N5,N9,E5,E6)` · **File:** [`IngSearch.tsx:14-32`](src/calculators/NutrizionaleCalc/IngSearch.tsx) vs [`recipeParser.ts:159`](src/utils/recipeParser.ts)

`searchDB` usa `nome.includes(query)`: "farina di frumento" restituisce **0 risultati** benché il DB contenga 8 voci con "frumento". Import Excel e Smart Import usano invece Fuse — due motori di matching diversi nello stesso tool.

**Fix applicato:** ogni parola della query deve comparire in `nome` **o** `etichetta`, in qualsiasi ordine, invece di cercare la stringa intera. Il ranking resta invariato. **Verificato live:** "farina di frumento" ora restituisce risultati invece di zero.

**Scelta:** match per sottostringa a token, non Fuse. Fuse avrebbe unificato i due motori ma richiede un indice in `useMemo` su 1065 voci a ogni keystroke e cambia l'ordinamento di tutta la tendina — sproporzionato rispetto al bug, che era sull'ordine delle parole, non sui refusi. **Resta aperta** l'opzione di unificare su Fuse: vedi *Da fare alla fine*.

### [x] N6 · "obbligatorio per scaricare" non applicato
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N3,N6,N7)` · **File:** [`DownloadTableModal.tsx:139`](src/calculators/NutrizionaleCalc/DownloadTableModal.tsx)

Con nome prodotto vuoto il modal si apre comunque e il PNG esce come `tabella - tabella UE - ….png`. La label `*` promette un vincolo che non esiste.

**Fix applicato:** corretta la **copy**, non aggiunto un blocco. Il nome prodotto non compare dentro la tabella ufficiale — i `Tab*` ricevono solo i valori — quindi serve al nome file e all'archivio: impedire l'export per un nome file sarebbe sproporzionato. Rimosso l'asterisco di obbligatorietà e placeholder da *"(obbligatorio per scaricare)"* a *"— usato per nome file e archivio"*.

### [x] N7 · Riepilogo con numeri illeggibili
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N3,N6,N7)` · **File:** `NutrizionaleCalc.tsx` (scheda Riepilogo) e `mobile/RiepilogoTab.tsx`

Valori resi come `100,000 g`, `100,000 g/pz`, `QUID 100,000%`: tre decimali con separatore virgola, si leggono come "centomila". Manca anche il simbolo `%` su alcune colonne.

**Fix applicato:** nuova `fmtQuantita()` in `shared/constants.ts` — **stessa precisione di prima** (3 decimali), zeri finali rimossi: `100 → "100"`, `12,5 → "12,5"`, `0,125 → "0,125"`. Nessuna informazione persa, solo rumore in meno. `fmt3` era duplicata identica in `NutrizionaleCalc.tsx` e `mobile/RiepilogoTab.tsx`: ora entrambe puntano alla funzione condivisa. Gli importi in € restano a 3 decimali fissi — per una colonna di soldi i decimali costanti sono più leggibili.

**Verifica:** 4 test (incluso il caso `1000` che la regex poteva ridurre a `1`); live il Riepilogo mostra `100 / 100 / 100 / 100` al posto di `100,000 / …`.

### [x] N8 · Accessibilità: controlli senza nome accessibile
**Stato:** ⬜ **NON CONFERMATO 2026-09-03 — era un falso positivo** · **File:** `NutrizionaleCalc.tsx`, `MobileShell.tsx`

- checkbox "Mostra valori facoltativi" e "Liquido" esposte come `checkbox "on"`
- input Porzione / U.V. / Pezzo con nome accessibile `—`
- **bottom tab bar mobile: 4 tab senza alcun nome accessibile**

**⬜ Falso positivo — nessuna modifica applicata.** Il finding nasceva dallo snapshot dell'albero di accessibilità dello strumento di ispezione, che mostrava il *placeholder* invece del nome calcolato. Verificato nel DOM reale:

| Controllo | Nome accessibile reale |
|---|---|
| checkbox facoltativi / Liquido | `input.labels` → "Mostra valori facoltativi", "Liquido" |
| input porzione | `labels` → "Porzione", "U.V./Conf.", "Pezzo" (con `id`/`htmlFor`) |
| tab bar mobile | `role="tablist"` + `role="tab"` + `aria-selected` + testo visibile |

L'accessibilità di questi controlli **era già corretta**. Lezione: verificare sempre nel DOM prima di correggere un finding di a11y basato su uno snapshot.

### [x] N9 · Schermata bianca durante il lazy load + naming incoerente
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N4,N5,N9,E5,E6)` · **File:** [`App.tsx:46`](src/App.tsx)

`<Suspense fallback={null}>` lascia la pagina bianca mentre il chunk carica. Inoltre lo stesso tool si chiama in tre modi: sidebar "Creazione tabelle valori nutrizionali", header desktop "Calcolatore Ricette", header mobile "Tabelle Nutrizionali".

**Fix applicato:** `fallback={<ToolLoading />}` con un testo "Caricamento strumento…" (`role="status"`, `aria-live="polite"`). Niente spinner: non esiste un keyframe `spin` nel progetto e non vale aggiungerne uno — il problema era la pagina bianca che sembra un crash, non l'assenza di animazione.

Header desktop allineato da "Calcolatore Ricette" a **"Tabelle Nutrizionali"**, il nome prevalente (`AppShell.tsx`, `ToolsTab.tsx`). La voce di sidebar resta "Creazione tabelle valori nutrizionali": è un'etichetta descrittiva di navigazione, non un secondo nome del tool. **Da confermare da te**, è una scelta di prodotto.

### [x] E5 · Paese di origine precompilato "Italia"
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N4,N5,N9,E5,E6)` · **File:** `EtichetteCalc.tsx` (default di `data.origin`)

Una dichiarazione d'origine di default, non scelta dall'utente, finisce in anteprima e negli export.

### [x] E6 · Doppia navigazione su mobile
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N4,N5,N9,E5,E6)` · **File:** `EtichetteCalc.tsx` + `MobileShell.tsx`

Tab in alto `Dati Etichetta | Grafica` e bottom bar `Dati | Anteprima | Grafica | Archivio` convivono e si sovrappongono per "Grafica".

### [x] E7 · Guida rapida riappare a ogni apertura
**Stato:** ⬜ **COMPORTAMENTO VOLUTO — nessuna modifica** · **File:** [`EtichetteCalc.tsx:857-858`](src/calculators/EtichetteCalc/EtichetteCalc.tsx), `onNeverShow` a `:3173`

Solo `onNeverShow` scrive `aea_welcome_seen_etichette`. Chiudere con ✕ non persiste nulla.

**⬜ Comportamento voluto — nessuna modifica.** Il modal ha un pulsante esplicito **"Non mostrare più"** accanto a "Chiudi": la ✕ è un rinvio, il pulsante è l'opt-out permanente. È un pattern intenzionale, non una dimenticanza, e non l'ho cambiato di mia iniziativa. Se lo trovi comunque fastidioso, far persistere anche la ✕ è una riga — vedi *Da fare alla fine*.

---

## ⚙️ Igiene di rilascio

### [x] T1 · Due test di sicurezza auth rossi in locale
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(N2,N2b,T1)` · **File:** `src/auth/AuthContext.test.tsx`, `.env.local`

Vitest legge `.env.local` → `VITE_DEV_MOCK_AUTH=true` attiva `DEV_MOCK_ENABLED` e bypassa il controllo, facendo fallire i due test AUTH-2. Verificato: con `VITE_DEV_MOCK_AUTH=false` passano 2/2.

**Fix applicato:** `test.env: { VITE_DEV_MOCK_AUTH: 'false' }` in `vite.config.ts`. Il mock resta attivo in sviluppo, i test girano sempre sul comportamento reale. **Suite: 182/182 verde.**

### [x] T2 · Errore di lint
**Stato:** ✅ CHIUSO 2026-09-03 — commit `fix(E3)` · **File:** [`EtichetteCalc.tsx:786`](src/calculators/EtichetteCalc/EtichetteCalc.tsx)

`'SubSection' is defined but never used` — 1 error, più 6 warning `react-hooks/exhaustive-deps`. Non blocca il deploy (`build` = `tsc -b && vite build`, lint fuori catena), ma sporca il gate qualità.

**Fix applicato:** rimosso il componente `SubSection` (46 righe, zero riferimenti in tutto il repo — verificato con grep su `src/`). `npm run lint` ora: **0 errors**, 6 warning invariati (`exhaustive-deps`, fuori scope). I 6 warning restano da valutare a parte: toccare le dipendenze degli hook senza capirne il motivo è come introdurre bug, non risolverli.

### [x] T3 · Richieste 401 duplicate a ogni mount
**Stato:** ⬜ **NON CONFERMATO 2026-09-03 — comportamento previsto** · **File:** `src/api/client.ts`, chiamanti in `NutrizionaleCalc.tsx` / `ArchiveModal.tsx`

`GET /api/ingredients/` e `GET /api/calc/archive/` partono 4 volte per mount e falliscono con 401 anche con mock auth attivo. Console rumorosa, lavoro inutile, nessun gate sul mock.

**⬜ Non confermato — comportamento previsto.** In `useIngredientsDB.ts` il 401 è il **fallback progettato**: `fromAPI().catch(() => fromStatic())`, con il commento *"in dev senza backend → fallback statico"*. Non è un errore non gestito: è il percorso che carica `/data/ingredientsDB.json`. La moltiplicazione delle chiamate è React **StrictMode**, che in sviluppo invoca gli effect due volte — non accade in produzione.

Resta un rilievo minore: con `VITE_DEV_MOCK_AUTH=true` la chiamata all'API è garantita fallire, quindi è un round-trip sprecato e rumore in console. Innocuo e solo in dev.

**✅ Rilievo minore risolto (T3b, 2026-09-03):** in mock auth non esiste alcuna sessione, quindi la chiamata è saltata e si va diretti al fallback — in `useIngredientsDB.ts` (file statico) e in `useArchive.ts` (`useBackend` falso → localStorage). Fuori dal mock il comportamento è invariato, fallback su 401 incluso. **Verificato live: nessuna richiesta a `localhost:8000` dopo il ricaricamento, console senza errori nuovi.**

---

## Log delle sessioni

| Data | Cosa è stato fatto |
|---|---|
| 2026-09-03 | Audit live eseguito, file creato, 18 finding aperti. |
| 2026-09-03 | Branch `fix/audit-2026-09-03`. **N1 chiuso** — guard ricetta vuota in `calcClaims` + 2 test, verificato live. |
| 2026-09-03 | **E2 chiuso** — menzioni obbligatorie Art. 9 con esenzioni per superficie e lotto, banner riformulato, avviso conservazione. |
| 2026-09-03 | **E1 chiuso** — leggibilità misurata sull'altezza della x (rapporto Arial 1062/2048) + 4 test. Effetto visibile: il default 100×150mm ora segnala invece di dare il verde. |
| 2026-09-03 | **E3 mitigato** (banner da verdi/accertati ad arancioni/condizionali) e **T2 chiuso** (rimosso `SubSection`, lint 0 errori). |
| 2026-09-03 | Sorgente Excel individuata (1057/1065 combaciano su `etichetta`). **D1 localizzato alle celle esatte**; scoperto **D2**: 9 prodotti "senza glutine" dichiarati con glutine presente, difetto che arriva fino all'app. |
| 2026-09-03 | **E3 chiuso** — campo superficie imballaggio, soglie derivate, retrocompatibile. **D1** tracciato alla sorgente reale (Excel esterno, non raggiungibile). |
| 2026-09-03 | **T1 chiuso** — `test.env` forza il mock a false: suite da 180/2 a **182/182**. **N2 e N2b chiusi** sui file protetti, con autorizzazione esplicita; 3 snapshot di guardia aggiornati dopo verifica del diff. |
| 2026-09-03 | **N4, N5, N9, E5, E6 chiusi**; **N8, T3, E7** verificati e archiviati come non-difetti. |
| 2026-09-03 | **N3, N6, N7 chiusi** — gate export su porzione mancante, copy nome prodotto onesta, `fmtQuantita` condivisa. |
| 2026-09-03 | **E4 codice fatto** — `ALLERGEN_PARENT` + `collectAllergenLabels` condivisa fra i 4 punti di aggregazione, modal ingredienti derivato dalla fonte unica, 6 test. Resta parziale: aperto **D1**, `grano saraceno` taggato `all_grano` nel DB. |

---

## Da fare alla fine — richiedono una tua decisione

Niente di quanto segue è stato applicato. Sono i punti che non posso chiudere da solo.

### [ ] N2 · Tab Australia inutilizzabile senza serving size
**Perché è qui:** il fix va in `TabAustralia.tsx`, uno dei `Tab*.tsx` **protetti**. La skill `normativa-alimentare` chiede *"autorizzazione esplicita per la singola modifica, non un via libera generale"*, quindi un "vai avanti" complessivo non basta.

**Modifica esatta che serve:** in `TabAustralia.tsx` le righe si costruiscono dentro `if (sv)`, cioè solo con la porzione. La colonna **Average Quantity per 100 g** non dipende dalla porzione ed è obbligatoria FSANZ. Va costruita sempre, lasciando vuote le sole due colonne per-porzione finché la porzione manca. Nessuna modifica a stili, bordi o font.

### [x] N2b · `1 servings per container` (plurale errato con valore 1)
✅ CHIUSO 2026-09-03. Singolare/plurale corretto in `TabUSA.tsx` (2 occorrenze) e `TabArabi.tsx` (1). Reso come **unico template literal** e non come espressioni JSX affiancate: così il DOM mantiene un solo nodo di testo, come prima.

### [ ] D1 · `grano saraceno` taggato `all_grano` nel DB
Vedi la scheda D1 sopra. Non posso modificare `ingredientsDB.json` (vincolo CLAUDE.md) e la correzione va fatta alla fonte che lo genera.

### [x] T1 · `VITE_DEV_MOCK_AUTH=true` in `.env.local`
✅ CHIUSO 2026-09-03 — nessuna scelta da fare, le due esigenze non erano in conflitto. Aggiunto `test.env: { VITE_DEV_MOCK_AUTH: 'false' }` in `vite.config.ts`: lo sviluppo continua a usare il mock, la suite verifica sempre il comportamento reale. **La suite passa da 180/2 a 182/182** e i due controlli AUTH-2 tornano a proteggere davvero.

### [ ] E3 — fix strutturale (superficie imballaggio)
Mitigato ma non risolto: serve il campo `packageLargestSurfaceCm2` + migrazione delle etichette archiviate. Dettagli nella scheda E3.

### [ ] E4 — sbloccare dopo D1
Il codice è fatto e testato. Va solo riverificato su una ricetta reale dopo la correzione dei dati.

### Opzionali — restano scelte tue

- **N5b** — unificare la ricerca manuale su Fuse, come l'import. Oggi sono due motori diversi: risolto il bug pratico, resta l'incoerenza.
- **N9b** — la sidebar dice "Creazione tabelle valori nutrizionali", il tool "Tabelle Nutrizionali". Coerente, ma se vuoi un nome solo dimmi quale.
- **E7b** — far persistere anche la ✕ della guida rapida, non solo "Non mostrare più".
- **6 warning `react-hooks/exhaustive-deps`** — restano da valutare uno per uno: toccare le dipendenze degli hook senza capirne il motivo introduce bug invece di risolverli.
